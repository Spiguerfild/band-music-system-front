import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'band-music-system-front',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
